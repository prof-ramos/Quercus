# Implementation Plans

Generated by the improve skill on 2026-09-18 (against commit `4d86a8c`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-dx-tooling-agents-baseline.md) | Estabelecer baseline de DX, linters (ruff, mypy), AGENTS.md e gitignore | P1 | S | — | DONE |
| [002](002-persistence-correctness-wal-timestamps.md) | Corrigir normalização UTC de timestamps, concorrência WAL e robustez de serialização | P1 | S | 001 | DONE |
| [003](003-data-model-memories-sessions.md) | Implementar schema e operações de persistência para study_sessions, memories e memory_evidence | P1 | M | 002 | DONE |

*Status values*: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (com motivo em uma linha) | `REJECTED` (com justificativa)

## Dependency notes

- **002 depende de 001**: O plano 001 estabelece `ruff`, `mypy` e `AGENTS.md`. Ter linters e checagem de tipos ativos é pré-requisito para garantir que as alterações no código de persistência em 002 e 003 passem nos gates de qualidade.
- **003 depende de 002**: O plano 003 expande o schema com `study_sessions`, `memories` e `memory_evidence`, reutilizando a normalização canônica UTC e a infraestrutura WAL/busy_timeout blindadas no plano 002.

## Findings considered and rejected

- **DOC-01 (Divergência na árvore de arquivos do `README.md` com `docs/`)**: Não justificado como plano isolado neste momento. Os documentos `ARCHITECTURE.md` e `MEMORY.md` serão criados organicamente conforme os módulos de memória forem implementados no código.
- **Índices adicionais prematuros em `events`**: Como auditado no commit `4d86a8c`, o repositório é single-user e de baixo volume inicial. Manter YAGNI até que haja volume de eventos que justifique tuning de índices específicos além dos primary keys e foreign keys.
